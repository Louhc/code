#include<bits/stdc++.h>
using namespace std;
#define i64 long long
#define fp( i, b, e ) for ( int i(b), I(e); i <= e; ++i )
#define fd( i, b, e ) for ( int i(b), I(e); i >= e; --i )

int T;

int main(){
	cin >> T;
	while( T-- ){
	}
	return 0;
}